# 🚀 Инструкция: Быстрый старт из Template_2026

Этот шаблон настроен по стандартам 2026 года для Android 15 (SDK 35) с исправлением 16KB выравнивания.

## Шаг 1: Клонирование
Скопируйте эту папку и дайте ей имя нового проекта.

Откройте её в Antigravity.

## Шаг 2: Настройка имен
В `app.json` обновите `name`, `slug` и `bundleIdentifier`.

В `package.json` измените имя проекта.

## Шаг 3: Установка и Генерация
Выполните: `npm install` (папку node_modules в шаблон не копировать!).

Сгенерируйте нативный код: `npx expo prebuild`.

## Шаг 4: Подключение Supabase
Свяжите проект: `npx supabase link --project-ref ВАШ_ID`.

Установите секреты: `npx supabase secrets set OPENAI_API_KEY=....`.

Деплой функций: `npx supabase functions deploy --no-verify-jwt`.

## Шаг 5: AI-Конституция
Агент автоматически следует правилам из `.antigravity/rules.md`.

*   **Запрещено**: Писать ключи в код.
*   **Обязательно**: Соблюдать SDK 35 и Safe Area.
